import Dashboard from "@/components/Dashboard";

const Index = () => {
  // This component now only needs to render the dashboard content.
  // The Header and Sidebar are handled by SharedLayout.
  return <Dashboard />;
};

export default Index;